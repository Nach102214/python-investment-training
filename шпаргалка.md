# 🚀 ШПАРГАЛКА ПО НЕОБХОДИМЫМ КЛАВИШАМ: NVIM & TMUX

Этот файл можно держать открытым в соседнем буфере (вкладке) Neovim.
Для переключения между вкладками используйте `Tab` и `Shift+Tab`.

---

## 🔹 ЧАСТЬ 1: ГОРЯЧИЕ КЛАВИШИ NEOVIM (Ваш личный конфиг)

> 💡 **ВАЖНО:** Клавиша `<leader>` в ваших настройках — это **ПРОБЕЛ**.
> Нажимайте Пробел, отпускайте его, а затем сразу нажимайте нужную букву.

### 📁 Управление файлами и вкладками
* `Пробел + e` — Открыть / закрыть дерево файлов (`NvimTree`) слева.
* `Пробел + ff` — Быстрый поиск любого файла в учебном проекте (`Telescope`).
* `Пробел + w` — Сохранить текущий файл (больше не нужно писать `:w`).
* `Пробел + q` — Закрыть файл / Выйти из Neovim.
* `Tab`         — Прыгнуть на следующую вкладку вверху экрана.
* `Shift + Tab` — Вернуться на предыдущую вкладку вверху экрана.

### 🐍 Запуск Python-кода и терминал
* `Пробел + x` — **Запустить текущий Python-файл.** Код автоматически сохранится, снизу откроется терминал, 
                 выполнит скрипт, а ваш курсор останется вверху в коде!
* `Esc`        — Закрыть нижнее окно терминала (работает из любого места, даже если вы пишете код вверху).
* `Ctrl + Стрелка Вниз` — Мгновенно перепрыгнуть курсором из кода вниз в терминал.
* `Ctrl + Стрелка Вверх` — Вернуться курсором из терминала вверх в код.

### 🛠️ Отладка кода (DAP по кнопке F5)
1. Наведите курсор на строку, где хотите поставить паузу, и нажмите `Пробел + b`.
2. Нажмите `F5` для старта. Экран разделится на панели отладчика.
* `F5`          — Запустить отладку / Продолжить движение до следующей точки.
* `F10`         — Сделать шаг вперед (выполнить следующую строчку кода).
* `F11`         — Шагнуть внутрь функции (если курсор стоит на её вызове).
* `F12`         — Шагнуть наружу из функции.
* `Пробел + b`  — Поставить или снять точку останова (Breakpoint).
* `Пробел + dr` — Перезапустить сессию отладки с самого начала.

### 🌐 Переводчик текста и Линтеры (Ruff / Pyright)
* `g + l`       — Показать системное окно с текстом ошибки на английском.
* `Пробел + g + t` — **Перевести техническую ошибку под курсором на русский язык** во всплывающем окне Google.
* `Пробел + t`  — Перевести слово под курсором (или выделенный текст) на русский.
* `Пробел + f`  — Перевести слово или выделенный текст на английский.
* `Пробел + r`  — (В режиме выделения текста `v`): Заменить русский текст английским переводом.
* `Пробел + fm` — Автоматически выровнять код в файле (исправляет отступы PEP8).
* `]d` / `[d`   — Прыгнуть к следующей / предыдущей ошибке в коде.

---

## 🔹 ЧАСТЬ 2: ГОРЯЧИЕ КЛАВИШИ TMUX (Стандартный классический набор)

> 💡 **ВАЖНО:** Любая команда в Tmux начинается с префикса **`Ctrl + b`**.
> Сначала зажмите `Ctrl` и нажмите `b`, затем отпустите их и нажмите следующую клавишу.

### 🪟 Разделение экрана на панели (Panes)
* `Ctrl+b` затем `%` — Разделить экран вертикально (на левую и правую части).
* `Ctrl+b` затем `"` — Разделить экран горизонтально (на верхнюю и нижнюю части).
* `Ctrl+b` затем `o` — Перепрыгнуть в следующую панель по кругу.
* `Ctrl+b` затем `x` — Закрыть текущую панель (потребуется подтвердить кнопкой `y`).
* `Ctrl+b` затем `z` — Развернуть текущую панель на весь экран (Повторное нажатие сворачивает её обратно). Очень удобно, 
           когда нужно детально изучить логи в терминале.

### 📑 Управление окнами-вкладками (Windows)
* `Ctrl+b` затем `c` — Создать новое чистое окно (вкладку на нижней панели).
* `Ctrl+b` затем `n` — Перейти на следующее окно.
* `Ctrl+b` затем `p` — Перейти на предыдущее окно.
* `Ctrl+b` затем `0...9` — Прыгнуть на окно по его номеру.
* `Ctrl+b` затем `w` — Вывести интерактивный список всех окон в виде дерева.

---

## 🔹 ЧАСТЬ 3: РЕКОМЕНДАЦИИ ПО ИСПОЛЬЗОВАНИЮ ОТ ИИ

1. **Разделяйте обязанности Tmux и Neovim:**
   Не нужно плодить панели в Tmux, если вам просто нужно запустить Python-код. Пользуйтесь вашим встроенным `<leader>x`. 
   Tmux используйте для глобальных задач: в одном окне Tmux запущен Neovim с учебой, во втором окне Tmux — системные утилиты 
   (например, `htop` или менеджер пакетов `pip`), в третьем — Git.

2. **Не закрывайте Neovim без необходимости:**
   Благодаря Tmux вы можете отключиться от сессии (нажав `Ctrl+b`, затем `d`), закрыть терминал ноутбука, уйти спать, 
   а утром открыть терминал, написать `tmux a`, и вернуть весь ваш рабочий процесс ровно на том символе, где вы остановились.

3. **Используйте отладчик (`F5`) вместо `print()`:**
   Когда вы учитесь работать со строками и списками, вместо постоянного написания `print(переменная)`, 
   ставьте точку останова через `<leader>b` на сложной строке и запускайте `F5`. Боковое меню переменных покажет вам состав 
   вашей строки/списка в тысячу раз нагляднее.

